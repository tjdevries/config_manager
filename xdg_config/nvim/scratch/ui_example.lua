
vim.ui = {
  -- This always returns one thing from the list.
  -- or perhaps even the index?
  pick_one = function(list_of_entries)
  end,
}

-- vim.ui.pick_one = telescope.pick_one
-- vim.ui.pick_one = external_process.something
